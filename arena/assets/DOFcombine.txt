uniform sampler2D scene;
uniform sampler2D pass2;
float focus = 0.48000;
float range = 4.60000;
varying vec2 texCoords;

void main(void)
{
   vec2 tcoord = vec2(1.0f - texCoords.s, texCoords.t); // Flip the x axis.

   vec4 sharp = texture2D(scene, tcoord);
   vec4 blur  = texture2D(pass2, tcoord);

   gl_FragColor = texture2D(scene, tcoord);
   //gl_FragColor = mix(sharp, blur, clamp(range * abs(focus - sharp.a), 0.0, 1.0));
}
